# MongoDB 连接字符串
# 格式: mongodb+srv://<username>:<password>@<cluster>.mongodb.net
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net

# 数据库名称（可选，默认为 profile_views_counter）
MONGODB_DB_NAME=profile_views_counter

# IP 访问时间窗口（小时）
# 同一 IP 在此时间内访问同一用户不重复计数
# 默认值: 0
IP_ACCESS_WINDOW_HOURS=0
